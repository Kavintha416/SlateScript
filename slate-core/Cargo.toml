[package]
name = "slate-core"
version = "0.1.0"
edition = "2021"

[dependencies]
# Other dependencies
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
rand = "0.8"
atty = "0.2"

[features]
default = []